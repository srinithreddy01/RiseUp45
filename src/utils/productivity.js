export const dateKey = (date = new Date()) => { const d = new Date(date); d.setMinutes(d.getMinutes() - d.getTimezoneOffset()); return d.toISOString().slice(0, 10) }
export const formatDate = (date, options) => new Intl.DateTimeFormat('en-US', typeof options === 'string' ? { weekday: options.includes('E') ? 'long' : undefined, month: options.includes('M') ? 'long' : undefined, day: options.includes('d') ? 'numeric' : undefined, year: options.includes('y') ? 'numeric' : undefined } : options).format(date)
export const makeId = () => `${Date.now()}-${Math.random().toString(36).slice(2, 7)}`
export function startOfWeek(date) { const d = new Date(date); const day = d.getDay() || 7; d.setDate(d.getDate() - day + 1); d.setHours(0,0,0,0); return d }
export function getStreak(tasks) { let streak = 0; const cursor = new Date(); for (let i = 0; i < 365; i++) { const key = dateKey(cursor); const dayTasks = tasks.filter(t => t.dueDate === key); if (dayTasks.length && dayTasks.every(t => t.completed)) streak++; else if (i > 0) break; cursor.setDate(cursor.getDate() - 1) } return streak }
export function nextOccurrence(value, recurrence) { const d = new Date(`${value}T12:00:00`); if (recurrence === 'Weekly') d.setDate(d.getDate() + 7); else { d.setDate(d.getDate() + 1); if (recurrence === 'Weekdays') while (d.getDay() === 0 || d.getDay() === 6) d.setDate(d.getDate() + 1) } return dateKey(d) }
