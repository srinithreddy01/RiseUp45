# Frontend

The existing Vite/React client remains in `src/`. Add page components in
`src/pages/`, reusable cards in `src/components/`, API adapters in
`src/services/`, and shared date/task helpers in `src/utils/`.

Run it with `npm --prefix frontend run dev`.